<resources>

    <style name="AppThemeDayNight" parent="Theme.AppCompat.DayNight">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="colorControlNormal">@color/colorAccent</item>
    </style>

    <style name="AppThemeDayNight.NoActionBar" parent="AppThemeDayNight">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar" />

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.NoActionBar.Translucent">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="BrandedSwitch" parent="AppTheme">
        <item name="colorAccent">@color/color_fab_active</item>
    </style>

    <!-- Login button style with explicit text color -->
    <style name="LoginButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/login_button_text</item>
        <item name="backgroundTint">@null</item>
    </style>

    <!-- Overlay theme for Login Inputs to force Green contrast in Light Mode -->
    <style name="LoginInputOverlay">
        <item name="colorControlNormal">@color/login_primary_green</item>
        <item name="colorControlActivated">@color/login_primary_green</item>
        <item name="colorAccent">@color/login_primary_green</item>
        <item name="android:textColorHint">@color/login_primary_green</item>
    </style>
</resources>
